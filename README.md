# نون الاحترافية — Noon Professionals

موقع الشركة، مبني بـ HTML/CSS/JavaScript خالص **بدون أي اعتماديات** ولا خطوة بناء.
الهوية البصرية (الشعار، الألوان، الخطوط) مطبّقة من دليل العلامة الرسمي.

---

## التشغيل

### الأسرع
افتح `index.html` بالنقر المزدوج. لا وحدات ES ولا `fetch`، فلا يحتاج خادمًا.

### عبر خادم محلي (مستحسن)
```bash
powershell -ExecutionPolicy Bypass -File serve.ps1
```
ثم <http://localhost:5177/> — لتغيير المنفذ: `-Port 5178`

> خادم على `System.Net.HttpListener`، لا يحتاج Node ولا Python.
> الملف بمحارف ASCII فقط لأن Windows PowerShell 5.1 يقرأ `.ps1` بترميز ANSI ويتعطّل مع العربية.

---

## الهوية البصرية

### الألوان
مأخوذة حرفيًا من دليل العلامة، ومعرّفة في `:root` أعلى `css/style.css`:

| الرمز | القيمة | الاستخدام |
|---|---|---|
| `--ink` | `#231f20` | النص الأساسي (ثانوي العلامة) |
| `--inv-bg` | `#000000` | الأقسام الداكنة (أساسي العلامة) |
| `--accent` | `#24baa9` | التيل — لون العلامة الأساسي |
| `--accent-2` | `#4dbd99` | الأخضر — أساسي ثانٍ |
| `--muted` | `#404041` | رمادي ثانوي |
| `--brand-grad` | `#24baa9 → #4dbd99` | التدرّج الرسمي |

كل اللوحات البصرية في الموقع (٦٨ لوحة) مشتقّة من هذه الألوان حصرًا.

### الشعار
`assets/brand/noon-mark.svg` — النسخة المضغوطة (V2) للترويسة
`assets/brand/noon-lockup.svg` — اللوحة الكاملة (V1) لشاشة التحميل

يُرسم الشعار كـ **قناع** (`mask`) فوق `currentColor`، لا كصورة:

```css
.md-logo__mark{
  background-color:currentColor;
  mask:url("../assets/brand/noon-mark.svg") center/contain no-repeat;
}
```

ولهذا يقلب لونه تلقائيًا من الأسود إلى الأبيض عند مرور الترويسة فوق قسم داكن، بملف واحد وبلا تضمين SVG في كل صفحة.

النسخ الأصلية كاملةً محفوظة في `assets/brand/V1__*.svg` و`V2__*.svg`.

### الخطوط
`itf Ghroob Arabic` — يغطّي العربية واللاتينية معًا حسب الدليل.

| الوزن | القيمة | الاستخدام |
|---|---|---|
| Light | 300 | التفاصيل الصغيرة |
| Regular | 400 | النصوص العامة |
| Medium | 500 | الفقرات الوصفية |
| Bold | 600–700 | العناوين الفرعية |
| ExtraBold | 800 | العناوين البارزة |

> **⚠️ تنبيه ترخيص:** ملفات `.otf` في `assets/fonts/` هي ملفات **سطح مكتب**. تضمينها في موقع عام يحتاج عادةً **ترخيص Webfont منفصلًا** من الناشر (International TypeFounders). راجع عقد شرائك قبل النشر.
>
> لتقليل الحجم لاحقًا: حوّلها إلى `.woff2` (توفّر ~٦٠٪) واستبدل `format("opentype")` بـ `format("woff2")` في أعلى `style.css`.

---

## فيديو الواجهة

`assets/video/showreel.mp4` — 1920×1080 · ٥٠ ثانية · ١.٧٢ ميجابت/ث · **١٠.٧ ميجابايت**

### تشغيل تلقائي مشروط

الفيديو **لا يُحمَّل للجميع**. القرار في `autoAllowed()` داخل `js/app.js`:

| الحالة | السلوك |
|---|---|
| حاسب + اتصال `4g` فأعلى | تشغيل تلقائي صامت بحلقة |
| جوال أو شاشة < 820px | لا تحميل — لوحة ثابتة وزرّ «تشغيل» |
| `saveData` مفعّل | لا تحميل |
| `2g` / `3g` | لا تحميل |
| `prefers-reduced-motion` | لا تحميل |

**المصدر في `data-src` لا `src`** — وهذا جوهري: المتصفح يبدأ التحميل بمجرّد قراءته لـ `src` في الترميز، أي قبل أن يعمل الجافاسكربت أصلًا، فتضيع فائدة الشرط كاملة. الجافاسكربت هو من يضبط `src` بعد اتخاذ القرار.

تحقّقت: على عرض 375px يبقى `video.readyState === 0` — **لم يُطلب بايت واحد**.

### الزرّ الأوسط — ثلاث حالات
- قبل البدء: **تشغيل** (مثلّث)
- يعمل ومكتوم: **الصوت** (سمّاعة مشطوبة)
- يعمل بصوت: **كتم** (سمّاعة بموجات)

الكتم إلزامي في التشغيل التلقائي — كل المتصفحات تمنعه مع صوت. أما النقر اليدوي فإيماءة صريحة تسمح بالصوت مباشرة.

يتوقّف الفيديو تلقائيًا حين يخرج من الشاشة.

### ⚠️ لتخفيفه فعليًا: اقصص لا تضغط
الترميز مقتصد أصلًا (١.٧٢ ميجابت/ث عند 1080p ترميز ممتاز). خفض معدل البت سيُفسد الصورة. المدة هي المشكلة:

```
١٢ ثانية × ١.٧٢ ميجابت/ث ÷ ٨ = ٢.٦ ميجابايت — بنفس الجودة تمامًا
```

```bash
ffmpeg -i showreel.mp4 -ss 0 -t 12 -c copy -movflags +faststart loop.mp4
```

`-c copy` = قصّ بلا إعادة ترميز ⇒ صفر خسارة. حينها يمكن تشغيله تلقائيًا للجميع وإلغاء الشرط.

> `faststart` مفعّل في الملف الحالي (`moov` قبل `mdat`)، ولذلك يبدأ التشغيل خلال ثانية ولا ينتظر التحميل الكامل.

---

## نظام المشاريع — مصدر بيانات واحد

المشاريع لم تعد مكتوبة داخل الصفحات. **`js/projects-data.js`** هو المصدر الوحيد، ومنه تُغذّى تلقائيًا:

| أين | كيف |
|---|---|
| الصفحة الرئيسية | أول ٦ مشاريع، بمعاينة الصورة الحقيقية في المؤشر عند المرور |
| صفحة الأعمال | كل المشاريع، شبكة كاملة |
| `project.html?id=04` | صفحة دراسة حالة واحدة تُقرأ حسب `id` في الرابط |
| "مشاريع أخرى" | تُحسب تلقائيًا (المشروع التالي بالتناوب الدائري) في كل صفحة مشروع |

### لإضافة مشروع جديد

1. ضع الصورة في `assets/work/<id>.jpg` (يُفضَّل ~١٢٠٠px عرضًا، مضغوطة)
2. أضف كائنًا جديدًا في `js/projects-data.js` بنفس البنية (`id`, `name`, `year`, `type`, `sector`, `scope`, `tags`, `lead`, `alt`, `sections`)
3. هذا كل شيء — يظهر في الصفحة الرئيسية وصفحة الأعمال وصفحته الخاصة وكل قوائم "مشاريع أخرى"، **بلا لمس أي HTML**

### كيف يعمل تقنيًا

الوحدة `Projects` في `js/app.js` تُصيَّر كل هذا في DOM عبر `innerHTML`، وتُستدعى **أول شيء** في `boot()` — قبل `Reveal.init()` و`Parallax.init()` وغيرهما. الترتيب هنا جوهري: تلك الوحدات تمسح DOM مرة واحدة عند الإقلاع بحثًا عن `[data-anim]`/`[data-parallax]`، فلو صُيِّرت بطاقات المشاريع بعدها لَما التقطتها الحركات.

قراءة `?id=` من الرابط تتم عبر `URLSearchParams(location.search)` — تعمل حتى عند فتح الملف مباشرة بنقرتين (`file://‎`) لأنها تحليل رابط محلي بحت، لا طلب شبكة.

### حدود معروفة

- **لا فهرسة SEO لكل مشروع على حدة** — محرّكات البحث التي لا تُنفّذ JavaScript ترى `project.html` فارغًا من محتوى مميّز؛ العنوان والوصف يُكتبان بعد التحميل. مقبول لموقع بلا خادم، لكن يستحق معالجة لاحقًا (توليد ثابت أو خادم يُصيِّر مسبقًا) إن أصبح مهمًا.
- ترتيب المصفوفة في `projects-data.js` = ترتيب العرض في كل مكان.

---

## بنية المشروع

```
├── index.html            الرئيسية — ١١ قسمًا
├── services.html         الخدمات
├── work.html             الأعمال — شبكة مُصيَّرة من projects-data.js
├── project.html          قالب دراسة حالة عام — project.html?id=04
├── about.html            عن الشركة
├── blog.html             المدونة
├── contact.html          نموذج تواصل
├── css/style.css         نظام الأنماط كاملًا
├── js/app.js             محرك التفاعل والحركة
├── js/projects-data.js   مصدر بيانات المشاريع الوحيد
├── assets/
│   ├── brand/            الشعارات
│   ├── clients/          شعارات العملاء
│   ├── fonts/            خطوط الهوية
│   ├── video/            فيديو الواجهة
│   └── work/             صور المشاريع
└── serve.ps1             خادم محلي اختياري
```

الترويسة والقائمة والتذييل مكرّرة في كل صفحة (لا يوجد نظام قوالب). عند تعديل أيٍّ منها طبّق التغيير على الملفات السبعة.

---

## محرك الحركة

مكتوب من الصفر في `js/app.js` — لا GSAP ولا Lenis ولا jQuery:

| الوحدة | ما تفعله |
|---|---|
| **Ticker** | حلقة `requestAnimationFrame` واحدة تغذّي كل الوحدات |
| **Scroll** | تمرير ناعم باستيفاء على التمرير الأصلي |
| **Cursor** | مؤشر مخصص بخمس حالات |
| **Reveal** | تقسيم النص إلى أسطر وكشف متتالٍ |
| **Marquee** | شريط لانهائي يتأثر بسرعة التمرير |
| **Magnetic** | عناصر تنجذب نحو المؤشر |
| **Parallax** | إزاحة محسوبة على موضع العنصر |
| **Feature** | قسم لاصق تتبدّل صورته |
| **Transition** | ستار انتقال بين الصفحات بلا وميض |
| **Drag** | سحب أفقي بالمؤشر أو اللمس أو العجلة |

### تفاصيل تقنية تستحق الانتباه

**التمرير الناعم لا يحرّك حاوية.** الطريقة الشائعة (`transform` على عنصر لاف) تُبطل `position:sticky`. هنا نعترض العجلة ونستدعي `window.scrollTo` بقيمة مستوفاة كل إطار، فيبقى التمرير أصليًا.

**RTL يعكس اتجاه الفيض.** في `dir="rtl"` يفيض المحتوى الأفقي يسار الإطار، فالإزاحة الموجبة هي التي تسحبه إلى الداخل.

**إحداثيات الفأرة فيزيائية دائمًا.** مواضع المؤشر وزرّ التشغيل وزرّ القائمة تستخدم `top/left` لا `inset-inline-start` — لأن `translate(-50%)` فيزيائي، واستخدام خاصية منطقية معه يزيح العنصر بعرضه كاملًا في RTL.

**تقسيم النص على مستوى الكلمة** لا الحرف، حفاظًا على اتصال الحروف العربية.

**لا `overflow:hidden` على `html`/`body`** لأنه يُبطل `position:sticky`؛ استُخدم `overflow-x:clip`.

**الحركة تُعطَّل** مع `prefers-reduced-motion`، والمؤشر والتمرير الناعم يُعطَّلان على أجهزة اللمس.

---

## الأمان والوصولية

### سياسة المحتوى (CSP)
كل صفحة تحمل `Content-Security-Policy` عبر `<meta>`. السياسة صارمة: `script-src 'self'` بلا `'unsafe-inline'`، فلا يوجد سكربت مضمّن واحد في المشروع. حارس وميض الانتقال — الذي يجب أن يعمل قبل أول إطار — نُقل إلى `js/transit-guard.js` بدل إضعاف السياسة.

> `style-src` يسمح بـ `'unsafe-inline'` لأن ألوان البطاقات واللوحات تُمرَّر عبر سمات `style`. هذا أضعف بكثير من السماح به للسكربتات.

### رؤوس الأمان
`serve.ps1` يُرسل `X-Content-Type-Options` و`X-Frame-Options: DENY` و`Referrer-Policy` و`Permissions-Policy`.

> **GitHub Pages لا يرسل أي رأس مخصص.** عند النشر هناك تعمل حماية Clickjacking من `frame-ancestors 'none'` داخل CSP فقط. لرؤوس حقيقية استضف على Netlify أو Cloudflare Pages وانقل نفس القيم إلى `_headers`.

### تنقية المخرجات
بطاقات المشاريع تُبنى بـ `innerHTML` من `projects-data.js`. كل حقل نصّي يمرّ على `esc()`، والألوان تُقبل بصيغة hex فقط، والمعرّف بأحرف وأرقام فقط. اختُبر بمشروع خبيث مُصطنع: صفر عناصر مُحقنة.

### تباين الألوان
شفافيات النصوص أُعيد ضبطها على WCAG 2.2 AA:

| الرمز | قبل | بعد |
|---|---|---|
| `--ink-70` | 4.94:1 | **9.13:1** |
| `--ink-40` | 2.44:1 ✗ | **4.63:1** ✓ |
| `--inv-70` | 7.85:1 | **13.77:1** |
| `--inv-40` | 3.39:1 ✗ | **6.27:1** ✓ |

### لوحة المفاتيح
- `:focus-visible` بحلقة `3px` بلون العلامة على كل عنصر تفاعلي (وبالأخضر فوق الأقسام الداكنة)
- رابط «تخطَّ إلى المحتوى» يظهر عند أول Tab
- القائمة `visibility:hidden` وهي مغلقة، فروابطها خارج ترتيب Tab
- Escape يُغلق القائمة ويفكّ قفل التمرير
- شرائح النموذج تحمل `aria-pressed` فتُنقل حالتها لقارئ الشاشة

---

## نقاط مفتوحة — تحتاج قرارك

1. **روابط التواصل الاجتماعي `href="#"`** — سبعة في كل صفحة (Behance · Dribbble · Instagram · LinkedIn). النقر لا يفعل شيئًا (مُنع القفز لأعلى الصفحة)، لكنها تبقى روابط بلا وجهة. أعطِ العناوين الحقيقية أو احذفها.
2. **أرقام غير حقيقية في عدّاد الرئيسية:** «١٢+ سنة» و«٢٤٠+ مشروع سُلّم حول العالم». «١٠٠+ عميل» وحده صحيح.
3. **نصوص عامة متبقّية:** «كيف نعمل» في الخدمات، و«محطّات» و«الفريق» و«كيف نفكّر» في صفحة عن نون، وآراء العملاء في الرئيسية — كلها من النسخة الأولى ولا تصف نون.
4. **بيانات التواصل مؤقتة:** `info@noonprofessionals.com` · `jobs@noonprofessionals.com` · `+966 50 000 0000` · العنوان.
5. **نموذج التواصل واجهة فقط** — لا يرسل شيئًا. يحتاج ربطًا بخدمة نماذج أو نقطة نهاية.
6. **الروابط المطلقة في SEO** مضبوطة على `mohanad19991212.github.io/madar-studio`. عند شراء نطاق، بدّلها في الصفحات الست و`sitemap.xml` و`robots.txt`.
7. **اسم المجلد والمستودع** ما زال `madar-studio`.
8. **ترخيص الخط** — انظر التنبيه أعلاه.
9. **الخطوط بصيغة OTF** (٣٨٠ ك.ب). تحويلها إلى WOFF2 يوفّر ~٦٠٪. وزنا Light و ExtraBold معرّفان في `@font-face` لكن لا يستعملهما أي عنصر — لا يُحمّلان إطلاقًا، فلا كلفة أداء، وتُركا جاهزين للاستخدام.

---

## الدعم

آخر إصدارين من Chrome و Edge و Firefox و Safari.
تُستخدم `mask` و`:has()` و`overflow:clip` و`color-mix()` — مدعومة في المتصفحات الحديثة وتتدهور بلطف.
